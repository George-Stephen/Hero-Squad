{{#partial "content"}}
    <h3>All Squads available</h3>
    {{#if Squads}}
     {{#each Squads}}
         <h3>{{squad_Name}}</h3>
     {{/each}}
    {{else}}
        No Squads recorded recently
    {{/if}}
    <a href="/Squads/form"><button>Add new Squad</button></a>
{{/partial}}
{{> layout.hbs}}